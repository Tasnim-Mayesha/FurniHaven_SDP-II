import 'package:flutter/material.dart';

class ShoeRackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shoe Rack'),
      ),
      body: Center(
        child: Text('Shoe Rack Page'),
      ),
    );
  }
}

