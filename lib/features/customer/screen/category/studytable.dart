import 'package:flutter/material.dart';

class StudyTablePage extends StatelessWidget {
  const StudyTablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Studytables'),
      ),
      body: const Center(
        child: Text('Studytable Page'),
      ),
    );
  }
}

