import 'package:flutter/material.dart';

class StudyTablePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Studytables'),
      ),
      body: Center(
        child: Text('Studytable Page'),
      ),
    );
  }
}

