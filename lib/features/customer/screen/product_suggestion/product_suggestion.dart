import 'package:flutter/material.dart';

class ProductSuggestion extends StatelessWidget {
  const ProductSuggestion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
